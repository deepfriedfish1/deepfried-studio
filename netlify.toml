[build]
  command = "ls -la node_modules/.bin/astro || echo 'astro not found'; npm install && npx astro build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200